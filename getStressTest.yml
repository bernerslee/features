config:
  target: 'http://localhost:3003'
  phases:
    - duration: 6
      arrivalRate: 1000
scenarios:
  - flow:
    - get:
        url: "/house/features/{{$randomNumber(0,9999999)}}"
    - get:
        url: "/house/interior/{{$randomNumber(0,9999999)}}"

